/**
 * GeoDirectory Plupload File Upload Script
 * Handles file uploads using Plupload library with Alpine.js for sortable thumbnails
 * Uses: Vanilla JS + Alpine.js + Alpine Sort + Bootstrap 5.3+
 */

// Import the main initialization module
import './plupload/index.js';
